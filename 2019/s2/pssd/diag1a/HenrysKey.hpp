#include <vector>
#include <string>
using namespace std;

class HenrysKey
{
    public:
    int howMany(vector<string> S)
    {
        int elements = S.size();
        int found[elements];

        for (int i = 0; i < elements; i++)
        {
            found[i] = 0;
        }

        for (int i = 0; i < elements; i++)
        {
            
        }

        

        return numOfDiff;
    }
};
